name: air_boltic_analytics
version: 1.0.0
config-version: 2

profile: air_boltic_analytics

model-paths: ["models"]
macro-paths: ["macros"]

models:
  air_boltic_analytics:
    staging:
      +materialized: view
      +schema: staging
    intermediate:
      +materialized: table
      +schema: intermediate
    marts:
      +materialized: table
      +schema: marts

